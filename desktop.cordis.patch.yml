# Desktop-only additive settings section. It does not alter or replace the
# official plugin inventory/settings rows shipped by dsh-web-app.
- insert:
    - id: ui-settings-desktop-personal
      name: '@local/dsh-client-ui-settings-desktop'

    # Uniform observable behavior across models (quiet | milestones | frequent):
    # a static communication-policy prompt section, defaulting to milestones.
    - id: agent-communication-policy
      name: '@local/dsh-agent-communication-policy'
